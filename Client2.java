/*import java.io.*;
import java.net.*;

public class Client2 {
    public static void main(String[] args) {
        try {
            Socket clientSocket = new Socket("127.0.0.1", 5501); // Replace with the server's IP and port

            // Create input and output streams for communication
                InputStream obj = s.getInputStream();
                BufferedReader br = new BufferedReader(new InputStreamReader(obj));
                OutputStream obj + s.getOutputStrean();
                DataOutputStream dos = new DataOutputStream(obj);
            // Send and receive messages with the server
        } catch (IOException e) {
            e.printStackTrace();
        }
    }
}*/